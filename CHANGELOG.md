# 更新记录

## 0.25.0（2026-09-19 · 内测 · 里程碑 4）

- **胶囊变小、只留看得懂的东西**：心形（跳动快慢就是你现在的心率，切走时变灰停跳）、心率、比平静高多少、**现在算在哪一段**——直接写成“生成中 / 读回复 / 打字 / 离开”，前面的小圆点和面板时间线同色。连了玩具时后面是玩具图标和路数。
- 去掉了心形外面的两圈环、背后的光晕、小折线和模式字（模式在面板里看）；胶囊高度 44 → 32。
- 设计说明：每个元素表示什么、为什么这样取舍，见仓库里的胶囊设计说明。

## 0.24.2（2026-09-19 · 内测 · 小修）

- **设备行更紧凑**：“手腕 / 胸前”就是标题行里那个小标签，点一下切换；“断开设备”改成标题行状态文字后面的一个电源图标。设置里不再重复这两行。
- **设置页不再放“这一版”的更新说明**，版本号只留在底栏。
- **设备实验室入口**：玩具页底栏常驻“设备实验室”（本机要先启动 device-lab）；设置页那一行也不再只在探测到时才出现（云酒馆是 https，本来就探不到）。

## 0.24.1（2026-09-19 · 内测 · 小修）

- 胶囊外圈“攒了多少”环原先比胶囊还高，上下露出胶囊边缘；心率圆盘缩小一点，两层环都收进胶囊里，相位环仍清楚露在圆盘外。
- README 截图换成 0.24。

## 0.24.0（2026-09-19 · 内测 · 里程碑 3）

- **心跳节奏跟着相位微调**：不只是换颜色——生成中等待时两次跳动隔得更开、更从容；打字专注时收紧一些；读回复阶段一旦判定出本轮峰值，那一拍的第二跳会明显放大一次，呼应“峰值点只画判定过的”。
- **模型判断回传的那一下**：模型读完这轮数据、写出的“读者信号判断”一到，胶囊（面板收起时）边框和光晕会同色闪一下，面板展开着的话改成判断文字区淡入高亮一下——不再是不出声的更新，看得出“数据出去了，读完了，传回来了”。只播一次，不会一直闪。
- **胶囊多一圈“攒了多少”环**：相位环外面再套一层更细的环，按这轮已经过去的时长和平常发送间隔的比例，从空长到满；下次发出去的瞬间归零，收缩闪一下。发送间隔沿用已经在学的阅读节奏数据，还没学够就先按 60 秒估。
- **平静心率旁边的呼吸环**：一个跟着平静心率自己呼吸的小圆环，周期和当前心跳是两个不同的钟——偏离越大，两个节奏看起来越不同步，不用盯着百分比数字也能感觉到。
- **离开的判定分了轻重**：只有切到别的标签页 / 锁屏这种协议里"真正不计入统计"的离开，才会整体变灰 + 心跳冻住；发一会儿呆、切到另一块屏幕这类弱信号，只是心跳跳动的幅度轻一点，颜色照旧。
- **相位环终于看得见了**：上一版环的外缘几乎全被胶囊圆盘挡住，这一版把环挪出来、描边加粗，胶囊看得清是哪种相位色。径向光晕也补上了跟着心跳呼吸的动效（之前只定义没接上）。
- 深色主题面板背景略微加深，正文灰字对比度回到 4.5:1 以上。
- 设置页“这一版”更新到 v0.24.0 · 里程碑 3。
- 所有新动效只动 transform / opacity / filter，遵守“减少动态效果”系统设置；不新增 rAF 循环，页面隐藏时不画。
- 测试 302 项全绿（`npm test`）。

## 0.23.0（2026-09-19 · 内测 · 里程碑 2）

- **面板换了材质**：健康设备页 + 胶囊改成磨砂玻璃质感（微透的深色渐变底 + 内侧一圈微光 + 一条渐变描边），更贴近悬浮组件该有的样子；深色 / 浅色两套配色都给了新颜色。**玩具页布局完全没动**，只是外层换成同一套材质，里面的按钮、动作列表、设备卡一个像素都没改。
- **心跳跟着当前心率走，离开时暂停并变灰**：胶囊上的心跳动画（两次跳动，像真的心跳）周期跟着当前显示的心率变化；外圈一个随相位换色的细环、一圈同步呼吸的光晕。协议判定你"不在"（切走、锁屏、久不操作）的那段时间里，心跳直接冻在当前帧、整个图标变灰，回来再恢复——呼应"这段不计入"的规则，不用看文字也能感觉到。
- **心率数字更平滑**：胶囊上滚动的数字和心跳节奏改用了更聪明的平滑算法（一种自适应滤波），平时不会跟着单个抖动的坏包乱跳，心率真的在变的时候又能跟得上，不像原来那样"迟钝或抖动二选一"。这一层只影响显示动画，块里真正判峰值、算百分比用的数据口径完全没变。
- **分段按钮改成滑动指示**：模式（幕后 / 入戏 / 知情）、设备戴在哪、节奏细调这几处分段按钮，点一下会看到一块颜色滑过去，不是硬切换。系统开了"减少动态效果"时自动退回原来的硬切换。
- 设置页"这一版"更新到 v0.23.0 · 里程碑 2。
- **修正（逻辑审查）**：
  - 关着流式输出点“重新生成”时，新回复里写的动作不执行（0.21.2 起的版本都有），已修。
  - 重新生成后，新回复里靠前的动作可能被当成“已经执行过”而漏掉，已修。
  - 一条回复动作很多时，没排完的动作会串到下一轮；现在你发下一条（或滑动、重新生成）时，上一条回复里还没开始的动作作废，正在做的做完。
  - 云酒馆（https）下不再每 20 秒探一次本机设备实验室（浏览器本来就会拦）；本机探不到时逐步放慢。
- 协议同步：TBC v0.3 §5.3 新增“新一轮开始时丢弃上一条回复还没开始的排队动作”。

## 0.22.0（2026-09-19 · 内测 · 里程碑 1）

- **设置页多了“这一版”**：写明当前版本号、属于哪个里程碑、改了什么。
- **设备实验室能从扩展里打开**：本机开着设备实验室（device-lab）时，「设置」里出现“本机的设备实验室 · 打开”，在小窗口里打开虚拟玩具、假手环和字节日志；没开时这一行不显示。
- **玩具部分暂缓，软性限制取消**（依据 DC 反馈）：每条回复的动作数不再限制（原来最多 5 个）、普通输出不再限单次时长（原来 30 秒）、两次之间的最短间隔可以调到 0。玩具界面保持不变。
  - 仍然保留：「全部停止」、断线即停；加热 / 微电流这类可能伤人的输出仍要在回复里点名，并保留它们的硬时长上限。
- 协议同步：TBC v0.3 §5.8 的每条回复上限改为 50（只防失控）。

## 0.21.3（2026-09-19 · 内测）

- **配色只手动**：不再根据酒馆主题自动判断深浅；「设置 → 外观」里一个「浅色面板」开关，缺省深色。以前选过“跟随酒馆”的按深色处理。
- 页面刚加载、还没画完时，不再露出占位的“-- 未连接 / -- 路”胶囊。
- 找不到设备时的提示改正：华为手环直接在手环上开始一个运动（如户外步行）就会广播心率，不需要手机 App（华为手环 11 实测）。
- 0.21.2 的修复全部保留（变量框架迟到事件、删光打字回看、设置标签页、点外面收起开关、提示句 3 轮后收起）。

## 0.21.2（2026-09-19 · 内测 · **不可用，请装 0.21.3**）

- 修：装了变量框架（MVU「额外模型解析」这类预设）时，生成还没完面板就提前显示「读回复」——变量框架写回旧消息、后台生成迟到的结束信号，不再被当成本轮回复到达。
- 修：打字打到一半全部删掉、回头看回复，那段时间现在算「读回复」，不再算「打字」；全都删了直接发送时，这一轮没有打字记录。
- 「更多设置」从弹层改成顶部第三个「设置」标签页（健康设备 · 玩具 · 设置），不会再有关不掉的问题；两个页面里的入口行已删。
- 新开关「点面板外面时收起」（设置页，默认开）：关掉之后，点面板外面不收起，用右上 × 或再点一下胶囊收起。
- 「下次发送时，模型会收到」的说明句聊过 3 轮后自动收起，只看「看原文」展开的完整块。

## 0.21.1（2026-09-18 · 内测）

- 修：酒馆主题把底色写成十六进制（如 `#F5F4F1`）时，浅色主题被误判成深色——玩具页设备卡变成深底深字、按钮描边偏深。现在十六进制和 rgb() 两种写法都认。

## 0.21.0（2026-09-18 · 内测）

- **“不在”判得更准**：从你按下发送就开始看，模型还在生成时切走也算离开；窗口切到别的程序（页面还看得见）也算不在；往上翻看旧消息时照算时长，但不再说“心跳落在这条回复的哪一段”。切走不到 5 秒、滚动经过旧消息不记。
- **不在就不算**：离开的时间不进统计；回来之后读的那一段照常判峰值，不会因为离开得久就整段放弃。回复写完时你不在，阅读时间从你回来那一刻算。
- **等回复时坐着不动不算离开**：还没出字时不判“久未操作”；出了新内容会重新计时，边看出字边读、不碰鼠标是正常读法。
- 点进卡片里的小界面（选项、状态栏）不会被当成切走。
- **门槛按你自己的节奏自动学**：多久没动算离开、读多久算太久，按你最近的干净轮悄悄校准；块里多一行 `gates` 写明用了什么值、是学的还是你手动设的。两个标签页的“设置与连接”多了「更多设置」，可以看这些门槛、手动改、改回自动、一键清除学到的习惯（只存本机）。
- **配色更准 + 可手动**：底色不透明时比底色和文字谁更亮来判深浅，半透明主题只看文字；还能手动选「跟随酒馆 / 深色 / 浅色」，给花背景留个出口。
- **更准的离开判断（可选）**：Chrome / Edge 上可开系统级空闲检测（锁屏算离开、卡片小界面里点按不算），要授权，缺省关。
- **节奏可细调**：每一档的最低强度、一次动多久、两次间隔、每条回复最多几个都能调；改了之后块里 `haptics` 行会告诉模型改了哪些。
- 「更多设置 → 试用」里可以开新版数据格式（v0.4 草案），不用再敲控制台。开着之后：
  - **模型知道回复是不是边出字边读的**：流式显示时，反应的位置按屏幕上已经显示到第几个字换算，不再拿“回看”那几秒当成正常阅读速度。
  - **心率的滞后写清楚了**：块里多一个“晚几秒”的值（胸带约 4 秒，手环/不认识的设备约 6 秒）；一个阶段刚开始就出现的峰值会标“延续自上一段”，提醒模型这更可能是上一段的余波，不是这段新出现的反应；一段结束后余波去了哪里、涨到多少，也会写出来。
  - **等回复、打字时也看得出峰值**：以前只有“读回复”这一段会给出明显的心跳高点，现在“模型生成中”“你在打字”这两段也一样给。
  - **不用自己心算了**：模型现在能直接看到这一段“平均多高”“比平静心率高了多少、持续了多久”“这一段里有多少秒你其实不在”，这些以前都要模型自己从数字里猜。
  - **等回复的时长按“你在不在”算**：以前只有读回复、打字这样处理，等回复的时长是从发送到收到回复整段照算；现在切走的时间同样不计入，回来后模型看到的时长更准。
- **注入更稳（每轮都注入、不重复、不串轮）**：
  - 生成中换聊天，上一个聊天还没用掉的注入块会被撤掉，新聊天的第一次请求不会带上别的聊天的心率。
  - 摘要、向量库、安静提示词这类后台生成插在你按下发送和提示词拼好之间时，这一轮不再丢块；后台请求本身仍然不带块。
  - 后台生成迟到的“生成结束”不再把你的新一轮提前结束（以前会提前撤块，读回复的起点也会记错）。
  - 发送后酒馆连不上服务器（“ST Server cannot be reached”）时留下的块，不会在一分钟后被卡片按钮发起的生成带走。
  - 群聊中途换聊天，或群聊状态残留半分钟以上，下一次普通发送按新一轮处理，不再被当成群聊成员而不注入。
  - 工具调用跑了超过 10 秒，之后的续生成仍算同一轮，不再多记一轮、不再多清一次反馈。
  - 两个注入接口都不可用时，这轮的摘要和反馈不再按“已送达”处理，接口恢复后下一轮补上。
  - 用控制台关掉注入（做实验用）期间按的反馈不再被清掉，恢复注入后的第一个块会带上。
- **玩具页“设置与连接”**：知道你的设备只支持一种连接方式时（比如只能浏览器直接连），另一个按钮会灰掉，补一句极短原因，位置不跳动；两种都支持或还不知道型号时照旧。

## 0.20.0

- **健康设备页改版**：设备名后标出戴在手腕还是胸前（设置里可改），三格信号表示最近一分钟收到多少心率，电量 ≤15% 标黄。
- **没戴好**：心率带报告没贴住皮肤时，这段心率不进统计，走势图上打琥珀斜线；告诉模型的记录里写明“这段没戴好”。
- **不再 30 秒就放弃重连**：设备走远或关了广播，就每 45 秒悄悄试一次，显示“等设备回来”，回来自动接上；点“断开设备”才停。刷新页面后可一键“重新连接 上次的设备”。
- **平静心率写明来历**：自动算的、手动记下的（几小时前）；超过一天或换了设备会提醒重记，发给模型时自动改用本场算出的值。新增“静坐 3 分钟记下”：前 1 分钟不算，期间打字或切走页面会从头来。
- 峰值口径统一：面板上的峰值点、“前几轮峰值”和发给模型的记录用同一个判定；没判出峰值的轮写“·”。
- 心率先过合理范围检查（0、300 之类的读数和单点跳变不再进统计）；胶囊上的百分比按最近 5 秒算，不再跟着单个读数跳。
- “下次发送时，模型会收到”只说记录里真有的内容。
- 去掉“发给模型”开关：扩展总是注入；以前关过的会自动恢复。
- 相位名改为 生成中 / 读回复 / 打字；说明里的原理图（加上“你的反馈”和“全部停止”）、相位图和两张面板截图换成新版。
- 玩具页玩法说明改成三步，附完整玩法链接；读法世界书新增“动作写法”一条，剧情联动打开时才生效，任何角色卡都能让设备动。

## 0.19.0

- **浏览器直接连支持我们自己的驱动**：不装 Intiface 也能连一部分 buttplug 不认识的设备。先选型号（繁野 啵啵贝、司沃康 SL278H，都标“未核实”；其它型号仍走原来的组件）。
  - 连接过程看得见：握手进度（收到几条 / 需要几条）、设备被官方 App 占着、握手超时，都有对应提示和重试。
  - 微电流这类有风险的输出默认关闭，要逐台设备打开并确认一次；加热、微电流仍必须在回复里点名才执行。
  - 断线不等于停下：设备卡会显示“可能仍在动”，重连后先把所有输出停掉。
  - 设备自带的、停不下来的模式（例如某些“失控模式”）要逐台开启，运行时一直提示“可拔出或按设备按钮”，并显示剩余时间。
- 设备卡：玩具电量（每 5 分钟读一次，低于 15% 标黄）、厂家部件名对照成中文、吮吸动效。
- “全部停止”改矮一点；胶囊在“可能仍在动”时显示琥珀色小点与倒计时。
- 回复结束改以“消息收到”为准（比渲染早，前端卡、变量框架插在中间时更准）。
- 开发版：把发出的动作报给本机设备实验室，时间线不用再按帧时序猜（发布版不带）。

## 0.18.0

- 界面第二轮：配色对比度全部达标，胶囊、走势图、时间线、设置区重新打磨；浅色主题单独调色；动效更顺，遵守“减少动态效果”。
- 玩具页新增快捷按钮：弱一点 / 强一点 / 再来一次 / 跳过；上一条回复的动作会标出“强 +20%”“已跳过”。
- 玩具页新增“设备”卡：每台设备的各路输出实时显示（振动波形、往复滑块、加热倒计时）。
- 玩具反馈进上下文（TBC §5.12）：你按停、调强调弱、换节奏、再来一次、跳过，以及设备自己停下，下一轮会以 `feedback(heartlink)` 行告诉模型；读法世界书加了对应读法。反馈本身不会触发新动作。
- 不打开面板也能停：斜杠命令 `/hl-stop`，快捷键 Alt+Shift+S。
- 注入块在本轮提示词生成后立刻撤下，别的插件之后发的后台请求不会带上它。
- 手机：拖动更顺；窄屏时面板铺满宽度；从没收到数据时显示“等待数据”；提示文字改为“电脑或安卓上的 Chrome / Edge，iPhone 暂不支持”；提示条合并，不再叠满屏幕；节奏弹窗按 慢热 → 极限 排序。

## 0.17.3

修复（来自相位与协议审查、换预设实测）：
- 预设让模型把思维链写进正文时，思维链里的 `<bio_act/>` 不再被执行，也不再占掉每条回复的动作名额；标签由扩展自己隐藏。
- 非流式回复里的动作现在会执行；中途停止的回复不执行动作；继续生成不会重复执行已经执行过的动作；小手机等插件的后台生成不会触发或吃掉动作。
- 切走页面的时间不再算进“读回复”；换页、群聊、工具调用、斜杠命令、请求报错、代写不再把相位切错。
- 注入块按协议 v0.3 定稿：`mode` 写旧值、`view` 写新值（幕后 / 入戏 / 知情）；没有样本的相位也输出合规的行；峰值与心率变异按新门槛判断。
- 其它扩展推来的文字会先检查、清洗，不能破坏注入块；外部推来的心率按它自己的时间戳记录，页面自己连着心率设备时忽略外部心率。
- 读法世界书带版本号，不会被旧版 heartlink 覆盖；发现旧版在改写时会提示。

## 0.17.2

- 性能：写聊天变量时不再额外上传整段聊天（每轮少 1–2 次整段上传），酒馆没保存时 10 秒后兜底保存一次。
- 性能：心跳动画不再触发整页重绘（主线程重绘从约 120 次/秒降到约 3 次/秒）。
- 性能：读法世界书改用酒馆缓存，只在切换聊天、世界书变化时检查，另每 10 分钟一次（原来每 30 秒请求一次）。
- 性能：面板每帧最多画一次，页面在后台时不画，内容没变不重写。

## 0.17.1

- 读法世界书：总说明改为只在注入块出现时点亮（不再常驻占预算）；模式条目与注入块放在同一深度，紧挨着数据。已在本地酒馆抓提示词确认：没有数据时不出现，有数据时模式说明紧跟在数据后面。
- 修复：只改了条目位置或触发词时，升级后世界书不会更新。

## 0.17.0

- 悬浮窗改版（本轮时间线）：看生成 / 读回复 / 写消息按时间线排开，各带小走势图，读回复的峰值是唯一的大数字；标题和底栏固定，中间滚动；浅色主题单独配色。
- 模式增加第三档**知情**（协议 `device-aware`）：角色知道你戴着设备，可以引用数字、指导你、明说是自己让设备动的；读法世界书新增对应条目。
- 玩具页：上一条回复的动作按时间线显示（已执行 / 正在动 / 排队中）；“在对话里这样玩”只在开始时显示，可关掉，底栏“玩法”可再打开；设置与连接可折叠，连上后连接按钮收成一行。
- 还没有数据时，“模型会收到”只显示一行提示；没有上一轮判断时不显示那一块。
- README 截图缩小。

## 0.16.1

- 原理图、相位图重画：统一配色（心率玫红、玩具青绿），字号分层，浅色 / 深色页面都清楚。
- 修复：玩具页的动作标签被挤到右边、一行一个。

## 0.16.0

- 节奏改为四档：慢热、持久、狂暴、极限。
- 去掉强度上限设置，默认按 100% 执行（“全部停止”照旧随时可用）。
- 精简面板里重复的说明文字。

## 0.15.0

- 健康设备页新增“下次发送时，模型会收到”：一句话说明这一轮会发给模型什么，“看原文”直接在面板里展开完整内容。
- 玩具页新增“上一条回复的动作”（每个动作的模式、强度、时长、执行结果）和“在对话里这样玩”说明。

## 0.14.0

- 相位显示重做：一条本轮心率走势，底色按相位分段（看生成再细分为等首字 / 思考 / 正文，读回复，写消息），页面切走的时段打斜线“不计”；下面每个相位一行，写时长、读回复的峰值、离开了多久。
- 面板里不再重复显示当前心率和百分比（胶囊上已经有）；平静心率只在虚线与下方一行出现。

## 0.13.0

- 健康设备页加入相位竖条：看生成 / 读回复 / 写消息各一根，显示这段时间心率的起伏与方向，正在进行的相位会标出来；虚线是平静心率。
- 平静心率只在一处显示，点“调整”记下或改回自动；去掉重复的卡片和按钮。
- 安全：“全部停止”和安全词会同时取消回复里还在排队的动作。
- 蓝牙：等设备回来的扫描可以真正停止；换设备时释放旧设备；“连着但没数据”试 3 轮后不再反复重连。
- 其它：没装本机桥时不再每分钟重试；浏览器不支持后台计时时，页面切走即停玩具；“代写”不再被当成新的一轮；长时间挂着时注入块不会变长。

## 0.12.0

- 悬浮窗重新设计：分段标签、大号心率与 3 分钟走势、数据卡片、开关用拨动开关、模式 / 节奏 / 上限用分段选择、醒目的“全部停止”。
- 统一叫“悬浮窗”；“隐藏悬浮窗”后从魔杖菜单恢复。
- 走势图里的虚线就是平静心率，“比平静 +N%”和它比；面板高度按屏幕可用空间自适应。
- 玩具连接改名：“通过 Intiface（推荐）”“浏览器直接连（不装软件 · 测试）”。
- README 加原理说明、流程图、相位图和手环兼容表。

## 0.11.2

- 按钮与文字重新命名：“连接设备”“发给模型”“记下平静心率”“剧情联动”“连接 Intiface”“蓝牙直连（测试）”“全部停止”等；不再用英文缩写。
- 开关只写名称，开着时显示青绿色；按钮改成每行两个。

## 0.11.1

- 悬浮窗菜单默认打开“健康设备”；只有真的连上玩具，胶囊才显示玩具那一段。
- 点“停”后收起菜单；菜单右上角加“×”收起。
- 可以隐藏悬浮窗，之后从酒馆左下角魔杖菜单的“显示 heartlink 悬浮窗”恢复。

## 0.11.0 — 首个公开版

- 酒馆扩展形态，不再依赖酒馆助手；读法世界书自动安装并设为全局。
- 悬浮窗分“健康设备 / 玩具”两个标签页，只显示正在用的那一类；按住可拖动。
- 心率蓝牙：自动重连（1、2、4、8、15 秒），之后等设备回到附近自动连回；显示已连接却没数据时自动重新订阅，仍没有则断开重连。
- 玩具：Intiface Central（buttplug v4，回退 v3）；直连β（buttplug-wasm，实验）；慢热 / 狂暴档位与自定义参数；设备逐路开关；强度帧计时放在 Worker 里，酒馆页在后台也不走样。
- 安全：默认关、强度上限、停止不经过模型、加热等有风险的输出必须点名、安全词可选（默认关）。
- 协议：Tavern Bio-Context v0.3（`haptics` 行、`tbc.outputState()`、`tbc.replyActs()`）。
